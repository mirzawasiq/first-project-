/* ===========================================================
   LIBERTY DRIVE — camera / control settings
   Tweakable at runtime from the console via LD.settings.
   =========================================================== */
LD.settings = {
  lookSensitivity: 0.0025,   // radians per pixel of mouse movement
  invertY: false,            // set true for inverted vertical look
  shoulder: 0.9,             // over-the-shoulder camera offset (0 = centred)
  camDistance: 7.4,
  camHeight: 4.0,
};
